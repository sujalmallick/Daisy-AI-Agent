# ====================================================================
# Daisy 🌼 — Environment Variables Configuration (.env.example)
# ====================================================================
# IMPORTANT: Never commit your actual .env file with real secrets!
# Copy this file to .env and fill in your actual credentials.
#
# Security notes:
#   - backend/config/custom_mcps.json  ← gitignored (may store API keys
#     in HTTP headers / webhook URLs for user-defined tools).
#     A default template lives at backend/config/custom_mcps.json.example.
#   - backend/config/voice_config.json ← gitignored (machine-local).
#   - .cache / .cache-*                ← gitignored (Spotify tokens).
# ====================================================================

# 1. Spotify Web API Credentials (https://developer.spotify.com/dashboard)
SPOTIPY_CLIENT_ID=your_spotify_client_id_here
SPOTIPY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIPY_REDIRECT_URI=http://127.0.0.1:8000/callback

# 2. Google Gemini API (https://aistudio.google.com/)
GEMINI_API_KEY=your_gemini_api_key_here

# 3. Voice & Hardware Configuration
# Options: auto (detects RTX 3050 CUDA -> DirectML -> CPU), cuda, directml, cpu
DAISY_HARDWARE_TIER=auto
DAISY_WAKE_WORD=Daisy

# 4. MCP Server Settings
MCP_ENABLED_SERVERS=spotify,filesystem

# 5. Filesystem MCP — directories Daisy is allowed to browse
# Comma-separated absolute paths, e.g.:
# DAISY_SEARCH_PATHS=C:\Users\YourName\Documents,D:\Projects
DAISY_SEARCH_PATHS=
